Создать в инвентарном файле 2 группы dbservers  и webservers, каждая из  не менее чем 2х хостов. Для каждой группы задать уникальное значение переменной для списка ntp серверов ntp_servers из 3х элементов,  каждая группа должна использовать  разные серверы, у любого одного хоста из любой группы ntp_servers должно отличаться от значений группы.
